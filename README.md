# discord-bot
Note badwords.json may include words that some people may find offensive, the point is that these words are removed from your chat.
Ensure you insert correct values in the config.json file. The bot will NOT work without doing so. Some functions require a database (Currently set up to use a MongoDB, you can get a free cloud instance at https://www.mongodb.com/cloud/atlas with up to 512MB)
